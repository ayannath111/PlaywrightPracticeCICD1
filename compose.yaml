services:
  playwright:
    image: mcr.microsoft.com/playwright:v1.61.0-jammy
    volumes: 
      - .:/app
    working_dir: /app
    #stdin_open: true
    command: bash -c "npm ci && npx playwright test Test1.spec.js"